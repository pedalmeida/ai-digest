# Lessons Learned

Non-obvious insights captured at session end. Newest at bottom.

- **2026-05-06** — Fragile string-based JSON truncation (lastIndexOf) fails when delimiters appear in string values; balanced-brace parser required for safety.
